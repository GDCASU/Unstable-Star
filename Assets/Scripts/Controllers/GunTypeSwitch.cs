using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GunTypeSwitch : MonoBehaviour
{
    //[Header("Shoot Initializer")]
    //[SerializeField] Shoot shoot;
    //List<string> laserChoices;

    //[Header("Laser Data")]
    //int currentLaserNumber = 0;

    //void Update()
    //{
    //    // Set laser inputs
    //    laserChoices = shoot.WeaponChoices;

    //    // Input system for switching guns
    //    if (Input.GetKeyDown(KeyCode.Q))
    //    {
    //        if (currentLaserNumber < laserChoices.Count){ currentLaserNumber++;  } // switches weapon to next
    //        else { currentLaserNumber = 0;  } // changes weapon back to initial
    //        shoot.SetWeaponFire(laserChoices[currentLaserNumber]); // finalizes the change
    //    }
    //    // Input system for shooting
    //    if (Input.GetMouseButtonDown(0))
    //    {
    //        shoot.TestShoot = true;
    //    }
    //}
}
